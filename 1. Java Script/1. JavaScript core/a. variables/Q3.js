//Guess the output:
console.log(count); // it will give undefined just because of hoisting declear variable at the starting but if it is var it assigned as undefined and in case of let and constant it is assigne to reference error 
var count = 42;  